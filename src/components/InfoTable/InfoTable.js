import React, { useEffect } from 'react';

const InfoTable = ({ notification, right }) => {

    return (
        <></>
        // <div id="modal-window" className={`${right} fixed z-50 top-5 duration-200 flex flex-col justify-center text-center text-purple border-2 border-purple bg-white rounded-10px h-70px w-150px`}>
        //     <div>{notification}</div>
        // </div>
    );
};

export default InfoTable;